import React from 'react';

function Filters() {
  return (
    <div className="filters">
      <button>vegetarian</button>
      <button>more</button>
      {/* <span>...</span> */}
    </div>
  );
}

export default Filters;